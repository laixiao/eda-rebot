---
description: 本板电路事实（以 EasyEDA Bridge 实时网表为准，2026-08-21 复查）
alwaysApply: true
---

# 本板电路事实（Bridge 实时网表）

当前活动工程：`AI通用机器人_v6`（文档偶称 v6-1，以 EasyEDA `friendlyName` 为准）/ Board `V1.0.0` / 原理图 `EDA-Robot` / 图页 UUID `1d774ca900623155`。  
**审查以实时网表为最终依据。**（相对 v5：已移除摄像头/SPI 屏/电机/编码器/第二路 PCA9685；雷达改为板载座；探照灯改 MOSFET 驱动。）

最新复查 **2026-08-21**：原理图 **73** 位号 / **56** 网络，DRC **2** warn（API 无逐条详情）；与 2026-08-09/08-11 基线一致（优先项全通过）。此前 **PCB↔SCH 网表零差异**（2026-08-09：`sch_Netlist` 侧比对 73/73 位号、56/56 网络）。

## 规模（2026-08-21 复查）

- **73** 个位号器件、**56** 条网络
- U1 **IO35/36/37** 无外设网络（Octal PSRAM，禁止使用）
- 原理图 DRC：**2** 条 warn（API 无逐条详情）

## 电源树

电池(U9) → VIN → Q5 AO4407C 高边（SW1 控制）→ VBAT → D1 SS56 → **8V** → U11 TMI3255 → **5V** → U5 LD1117 → **3V3**（单一网名 `3V3`，无 `3.3V`）

- 8V 额外用途：白光负载 U12/U14、舵机座不走 8V（舵机 T3/T4 为 5V）、T5 测点含 8V/3V3/GND

## I2C（OLED_SDA / OLED_SCL）

| 设备 | 地址 | 说明 |
|---|---|---|
| XL9555 U13 | 0x20 | A0/A1/A2=GND；INT#→U1 IO2（R4↑3V3） |
| OLED U10 | 0x3C | |
| PCA9685 U16 | 0x40 | A0..A5=GND；舵机 + 探照灯 PWM |

U1：IO12=SDA，IO13=SCL；R11/R12 4.7k↑3V3。  
**已无 U23。**

## ESP32 外设（U1）

| 功能 | 连接 |
|---|---|
| 麦克风 INMP441 U2 | SCK=IO16，WS=IO17，SD=IO18；L/R→MIC_LR→R2↓GND；Web「录音」`/api/rec` |
| 功放 MAX98357 U3 | LRC=IO38，BCLK=IO39，DIN=IO40；GAIN→R8↓GND；SD=`SPK_SD`→XL9555 IO1_6 + R10↑3V3；VIN=5V；Web「扬声器」`/api/play`；数字音量 `/api/amp` `volume` 0..100 |
| 60G 雷达 UART | IO9=`Radar-RX`，IO10=`Radar-TX` → U6 3P 座 |
| GPIO 扩展排针 U4 | IO4/5/6/7/41/42 |
| 下载 | T1=BOOT/EN/3V3；T2=GND/RX/TX（UART0） |

U1 当前 NC：IO1/8/11/14/15/19/20/21/35/36/37/47/48。

### Strapping

| GPIO | 保障 |
|---|---|
| IO0 BOOT | R1 10k↑3V3 |
| IO3 | R7 10k↓GND |
| IO45 | R15 10k↓GND |
| IO46 | R9 10k↓GND |

## 雷达（板载座，非飞线）

| 座 | 网络 |
|---|---|
| U6 3P | 1=`Radar-RX`→U1 IO9，2=GND，3=`Radar-TX`→U1 IO10 |
| U7 3P | 1=`RADAR_VCC`，2=GND，3=`Radar-OUT`→XL9555 IO0_0 |

- 供电：Q4 AO3401（P-MOS）S=3V3、D=`RADAR_VCC`、G=`IO0_1`；R16 10k↑3V3 → **默认关电**（需 XL 拉低 IO0_1 才供电）
- **命名实测（2026-08-09）**：模块 TX 实际落在网名 `Radar-TX`=IO10、模块 RX 落在 `Radar-RX`=IO9；固件按 `RX=IO10 听模块 TX / TX=IO9 发往模块 RX`（原「模块 TX→Radar-RX」注释与实际硬件不符，已按实测修正）

## XL9555（U13）

| 脚 | 网络 |
|---|---|
| IO0_0 | Radar-OUT |
| IO0_1 | 雷达电源使能（拉低=开，经 Q4） |
| IO0_2..4 | 扩展开关 → U8.1..3 |
| IO0_5 | NC |
| IO0_6 | U16 OE#（R23 10k↑3V3，默认禁 PWM） |
| IO0_7 | NC |
| IO1_0..5 / IO1_7 | NC |
| IO1_6 | SPK_SD |

## 舵机 / 探照灯（仅 U16）

- 舵机：T3=`SERVO_1`→U16 LED11；T4=`SERVO_2`→U16 LED12（座电源=5V）
- **T4 云台底座**：行星齿轮 **2:1**（舵机 180° → 底座机械 360°）；追踪映射 `舵机角 = 90 + 雷达方位/2`
- 探照灯 PWM：LED0=`LED_ALL`，LED1=`LED_1`，LED2=`LED_2`，LED3..5→U8.4..6
- 功率级（8V 白光）：
  - U12 / Q1 / D3：`LED_2` 驱 AO3400，低边经 `LED_GND`
  - U14 / Q2 / D2：`LED_1` 同上
  - Q3：`LED_ALL` 控 `LED_GND`→GND（总开关）
  - 门极 R13/R14/R19↓GND，默认关
- 扩展座 U8：IO0_2/3/4 + LED_3/4/5
- 机械件 M1=SG90 示意；SCREW1..4→GND，Convert to PCB=yes、Add into BOM=yes

## 相对 v5 已移除（固件若仍调用需停用或改适配）

摄像头 OV5640、SPI ST7796/XPT2046、TB6612 电机、ENC1..4 编码器计数、PCA9685 U23、舵机 T5–T7。

## 固件约定（`firmware/` FW ≥ 3.6.15，对齐 v6-1）

- 分区：`factory` 1MB 救援 + `ota_0` 14MB 主系统（模型内嵌 `main/srmodels.bin`，无独立 model / 无双 OTA）
- 构建主系统：`cd firmware && idf.py build`；救援：`cd firmware_recovery && idf.py build`
- 首次改分区：串口 `scripts/flash_all.ps1 -Port COM3`；日常：主页「进入救援升级」→ 救援页上传大包
- **设置持久化（NVS `cfg/*`，FW≥3.6.16）**：语音、音量、PWM/功放/雷达供电、控风扇/手势/自动追踪、档位与记忆强度、LED 占空比、舵机角度、雷达每日时刻、**忽略扇区**（`rign_en`/`rign_n`/`rigns` blob，FW≥3.6.26 多段；旧键 `rign_a`/`rign_b` 仍作第 0 段兼容，默认区间 0°～-60°）；上电按 NVS 恢复。急停清运行态但保留 `cfgSnap*` 意图镜像（避免随后改音量把 NVS 写成全 0）；解除急停时按 snap 恢复 LED/舵机；自动风扇/手势改输出约 2s 防抖落盘
- 上电：先按安全默认再 `cfgApplyBoot` 恢复；**缺焊 I2C 器件不阻断启动**（probe 失败则不 begin、后台不做 I2C；FW≥3.6.3）
- 空板可只跑 WiFi/HTTP；语音默认关（无 NVS 记录时）；I2S DMA 空闲关闭
- 雷达：`POST /api/radar {"power":true}` 开电即自动查询（已取消独立采集开关）；UART IO9/10
- **忽略扇区**：网页「忽略扇区」可 **＋ 添加 / × 删除** 多段（最多 8）；`POST /api/radar {"ignoreEnable":true,"ignoreAdd":true,"ignoreFrom":0,"ignoreTo":-60}` / `{"ignoreDel":0}` / `{"ignoreId":0,"ignoreFrom":10,"ignoreTo":20}`；目标不参与控风扇/手势切档/自动追踪，画布仍显示；NVS `cfg/rign_en` `rign_n` `rigns`
- 舵机 id `0..1`；探照灯 id `0/1/2`=LED_1/2/ALL（1/2 需 ALL）
- 雷达→风扇：`POST /api/fan {"auto":true}`；每 **2s** 取样；**有人 1 次即开**、**无人连续 3 次才关** **LED_1**；开时按手势档位（若手势开）或记忆强度；有人判定不含单独呼吸/微动/OUT；默认关
- 手势切档：`POST /api/fan {"gesture":true}`（可与控风扇**同时开**）；近距 **≤0.20m** 停留 **2s** → 档位循环 **50%→75%→100%**（sticky `gear`）；切成功**无提示音**（FW≥3.6.17）；只管档位、**不关闭控风扇**；自动再开时用当前档位；触发后须离开（**>0.35m**）再武装；默认关
- **自动追踪（FW≥3.6.28）**：`POST /api/fan {"track":true}`；网页「自动追踪」；**雷达墙装固定、不随云台转**（方位为房间绝对角）；雷达方位→**T4**（2:1 齿轮：`舵机=90+方位/2`，舵机 180°=底座 360°）、距离估仰角→**T3**（雷达无俯仰角）；尊重忽略扇区；需雷达供电；自动开 PWM；NVS `cfg/ftrack`；默认关。**零点**：约定雷达 0° ↔ 舵机 90° ↔ 底座正前；若装反左右可改符号，若偏置需再标定。
- **栈教训**：`background_task`（4KB）禁止同步 I2S 播音；见 `.cursor/rules/firmware-bg-audio.mdc`
- 语音控风扇（ESP-SR）：默认关（NVS `cfg/voice`）；网页「语音」开关或 `POST /api/voice {"on":true|false}`；开后 **MultiNet 伪唤醒「你好爱妃」**（无 WakeNet；随机播放 `firmware/assets/*.wav`，失败则双音）→ 开/关风扇、**大一点/小一点（±20%）**、最大/最小(20%)/中等(50%)；开或加大时恢复/设置 LED_1×LED_ALL 并**关闭雷达联动**；`GET /api/voice` / `/api/status` 含 `enabled`/`modelBytes`
- 录音：`POST /api/rec {"on":true|false}`，`GET /api/rec/wav`；播放录音 `POST /api/play`；上传 `POST /api/play/upload`（WAV PCM16 @16kHz）
- 音量：`POST /api/amp {"volume":0..100}`（数字缩放，默认 100；仅改音量不碰功放开关）；`/api/status` 含 `volume`；影响 play/beep/upload
- 已删：摄像头 / LCD / 电机 / 编码器 / U23 API
