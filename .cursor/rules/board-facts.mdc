---
description: 本板电路事实（以 EasyEDA Bridge 实时网表为准，2026-07-26 复查）
alwaysApply: true
---

# 本板电路事实（2026-07-26 Bridge 实时网表）

当前活动工程：`AI通用机器人_v6` / Board `V1.0.0` / 原理图 `eda-robot` / 图页 `P1` UUID `1d774ca900623155`。  
**审查以实时网表为准。**

## 规模（2026-07-26 本次复查）

- **70** 个位号器件、**42** 条网络；原理图 DRC 本次返回空数组
- U1 **IO35/36/37** 无外设网络（Octal PSRAM，禁止使用）
- **已移除**：SPI 屏 U4、第二路 PCA9685 **U23**、舵机 T5–T7、安装孔 M2；探照灯并入 U16
- **已补回 strapping 下拉**：IO3←R7 10k→GND，IO46←R9 10k→GND，IO45←R15 10k→GND
- 摄像头 / 电机驱动 / 编码器：仍无
- 安装孔：仅 **M1** + SCREW1–8（无 M2–M5）

## 电源树

电池座(U9) → VIN → Q5 高边开关 → VBAT → D1 → 8V → U11 → **5V** → U5 LD1117 → **3V3**（统一网名 `3V3`，无 `3.3V`）

## I2C（OLED_SDA / OLED_SCL）

| 设备 | 地址 | 说明 |
|---|---|---|
| XL9555 U13 | 0x20 | A0/A1/A2=GND；INT#→U1 IO2（R4 上拉） |
| OLED U10 | 0x3C | |
| PCA9685 U16 | 0x40 | A0..A5=GND；舵机 + 探照灯 |

U1：IO12=SDA，IO13=SCL；R11/R12 上拉。~~U23 0x41 已删除~~。

## ESP32 外设（U1）

| 功能 | 连接 |
|---|---|
| 麦克风 INMP441 U2 | SCK=IO16，WS=IO17，SD=IO18 |
| 功放 MAX98357 U3 | LRC=IO38，BCLK=IO39，DIN=IO40；SD=`SPK_SD`→XL9555 IO1_6 |
| SPI 显示 U4 | **已移除**（IO3/45/46 仅作 strapping，无外设） |
| UART0 | RXD0=`RX`，TXD0=`TX`（下载排针 T2：GND/RX/TX） |
| BOOT/EN | T1：BOOT/EN/3V3；IO0=`BOOT`←R1 10k↑3V3；EN←R3 10k↑3V3 |
| 60G 雷达 ×1 | 见下节（网名 `Radar-RX` / `Radar-TX` / `Radar-OUT`） |
| 摄像头 / 电机 | **已移除** |

## 60G 雷达 ×1（MS60-1211S80M）— 实时网表 + 手册核对

模组手册脚序（自上而下）：**左** OUT/GND/VCC，**右** TX/GND/RX。供电 3.0~5.5V / ~80mA；官方飞线板默认 **3.3V**。座子 U6/U7=`2005WV-3P`（C6837594）。原理图上 U6 在左（x≈945）、U7 在右（x≈985，旋转 180°）。

### 当前网表（不可 1:1 插模组）

| 座（示意位置） | 脚 | 当前网络 | 对端 |
|---|---|---|---|
| **U6**（左） | 1 | `Radar-RX` | U1 **IO9** |
| **U6** | 2 | GND | GND |
| **U6** | 3 | `Radar-TX` | U1 **IO10** |
| **U7**（右） | 1 | **3V3** | 3V3 |
| **U7** | 2 | GND | GND |
| **U7** | 3 | `Radar-OUT` | XL9555 **IO0_0** |

**严重：** 左座应接 OUT/GND/VCC，现却是 UART；右座应接 TX/GND/RX，现却是 3V3/GND/OUT。1:1 对接会把模组 OUT 接到电源、把 UART 接到左座。VCC 已从 5V 改为 3V3（供电更合理），但左右功能对调仍未修。

### 应改成（与手册一致，供用户手改）

| 座 | 脚 | 网络 | 对端 |
|---|---|---|---|
| **U6**（左） | 1 | `Radar-OUT` | XL9555 **IO0_0** |
| **U6** | 2 | GND | GND |
| **U6** | 3 | `3V3`（或带开关的 `RADAR_VCC`） | 电源 |
| **U7**（右） | 1 | `Radar-RX` | U1 **IO9**（←模组 TX） |
| **U7** | 2 | GND | GND |
| **U7** | 3 | `Radar-TX` | U1 **IO10**（→模组 RX） |

去耦/通断（建议）：电源脚旁 **100nF+10µF**；可选 `5V`→SGM2551→`RADAR_VCC`，EN←XL9555 **IO0_1**（现 NC）。无开关时固件总开关只停采集、不断电。

固件（已实机验证协议）：UART1 **115200 8N1**，RX=IO9 / TX=IO10，OUT→XL **IO0_0**（`begin` 将 port0 配成 `0x9F`，bit0=输入）。改脚后固件引脚映射无需改。

## XL9555（U13）

| 脚 | 网络 |
|---|---|
| IO0_0 | `Radar-OUT` |
| IO0_1..5、IO0_7、IO1_0..5、IO1_7 | NC |
| IO0_6 | `IO0_6` → U16 OE#（R23 10k↑3V3） |
| IO1_6 | SPK_SD |

## 舵机 / 探照灯（仅 U16 @ 0x40）

- 舵机：**仅 T3/T4** → `SERVO_1`/`SERVO_2` → U16 **LED11/LED12**（T5–T7、LED13–15 已无）
- 探照灯：U16 LED0=`LED_ALL`，LED1=`LED_1`，LED2=`LED_2`
  - Q3：G=`LED_ALL`，S=GND，D=`LED_GND`（总地开关）
  - Q2：G=`LED_1`，S=`LED_GND`；Q1：G=`LED_2`，S=`LED_GND`
  - 灯座：U12/U14=`KF350-3.5-2P`（5V + 各通道漏极）
- U16 LED3–10、LED13–15：NC

## U1 Strapping

| GPIO | 网络 | 保障 |
|---|---|---|
| IO0 | BOOT | R1 10k → 3V3 |
| IO3 | IO3 | R7 10k → GND |
| IO45 | IO45 | R15 10k → GND（保 VDD_SPI=3.3V） |
| IO46 | IO46 | R9 10k → GND |

## 固件约定（`firmware/`，ESP-IDF FW ≥ 2.2.0）

- 构建：`cd firmware && idf.py set-target esp32s3 && idf.py build`（需 ESP-IDF ≥ 5.3）
- 探照灯现挂 **U16 0x40** LED0–2（勿再写 U23 0x41）；`LED_ALL` 为 Q3 总开关
- 舵机仅通道 LED11/12（SERVO_1/2）
- `/api/servo` / `/api/led` 须先 `/api/pwm?on=1`
- `/api/ota`：GET 分区信息；POST raw `.bin` Web 烧录
- `/api/lcd` `/api/touch`：硬件已删，勿当作本板可用
- camera / motor / encoder：硬件已删；radar 按上表 IO9/IO10 + XL IO0_0
